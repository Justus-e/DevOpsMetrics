apiVersion: v1
kind: Service
metadata:
  labels:
    {{- include "devopsmetrics.labels" . | nindent 4 }}
    app.kubernetes.io/component: webapp
  name: {{ template "devopsmetrics.fullname" . }}-webapp
spec:
  type: {{ .Values.webapp.service.type }}
  selector:
    {{- include "devopsmetrics.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: webapp
  ports:
    - protocol: TCP
      port: {{ .Values.webapp.service.port }}
      targetPort: {{ .Values.webapp.service.port }}
      {{if eq .Values.webapp.service.type "NodePort"}}
      nodePort: {{ .Values.webapp.service.nodePort }}
      {{end}}
     
