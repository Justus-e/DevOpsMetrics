{{- if .Values.networkpolicy.enabled -}}
kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: grafana-netpol
spec:
  ingress:
    - ports:
        - port: 3000
          protocol: TCP
  podSelector:
    matchLabels:
      app: grafana
{{- end }}